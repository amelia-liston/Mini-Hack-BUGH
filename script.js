var button = document.getElementById("redirectButton");

        // Add a click event listener to the button
        document.getElementById("OrderForDelivery").addEventListener("click", function() {
            // Redirect the user to a different page
            window.location.href = "order.html"; // Replace with the URL you want to redirect to
        });